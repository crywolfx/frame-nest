# 公开数据边界

页面只保留旅行者明确要求公开到攻略中的航班号、酒店名称、日期和固定门票时间。

- 不保存小红书分享令牌、Cookie、浏览器配置、登录状态或浏览历史。
- 小红书仅用于经验提示；票价、时刻和票制由京成官网复核。
- Google Maps 仅用于读取公开评分、价格区间、营业与近期排队体验；不发布用户账户数据。
- 所有公开 URL 只允许 `http` 或 `https`，不得携带账号密码。
- 地图使用独立的 `tokyo-summer-2026-*` 本地存储键，不读取其他攻略状态。
- `travel-library/` 为本地素材层并被 Git 忽略。
